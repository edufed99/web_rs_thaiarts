# Private Model Service (issue #10): after Next.js achieved full public
# application-surface parity, the FastAPI package contains only the
# authenticated private model contract (health + inference + similarity).

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Settings + schemas
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Data + ML (numpy + pandas already needed by pipeline; backend reads parquet)
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0

# E5 encoder — ~2.5 GB, downloaded from HuggingFace on first use.
# Set RECSYS_E5_ENABLED=0 in CI / tests to skip model load entirely.
sentence-transformers>=3.0.0
torch>=2.1.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27.0
