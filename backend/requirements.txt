# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# FastAPI requires python-multipart for any ``UploadFile`` route
# (the dep check fires even if the field is unused).
python-multipart>=0.0.7

# Settings + schemas
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Data + ML (numpy + pandas already needed by pipeline; backend reads parquet)
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0

# Database (PostgreSQL via SQLAlchemy + psycopg sync driver)
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Auth slice (admin ingest — see ADR §11.1)
# bcrypt is used directly (no passlib wrapper) to avoid the 72-byte password
# truncation footgun in older bcrypt bindings.
bcrypt>=4.1.0
pyjwt>=2.8.0
cryptography>=42.0.0

# Live-ingest slice (admin adds new items at runtime — see ADR §3)
# E5 model is ~2.5 GB; downloaded from HuggingFace on the first ingest.
# Set RECSYS_E5_ENABLED=0 in CI / tests to skip model load entirely.
sentence-transformers>=3.0.0
torch>=2.1.0
google-generativeai>=0.7.0
pythainlp>=4.0.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# In-memory SQLite for fast tests
aiosqlite>=0.20.0