# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Settings + schemas
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Data + ML (numpy + pandas already needed by pipeline; backend reads parquet)
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0

# Database (PostgreSQL via SQLAlchemy + asyncpg)
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# Password hashing for user auth (used by /api/users endpoints)
passlib[bcrypt]>=1.7.4
# python-jose for future JWT support
# python-jose[cryptography]>=3.3.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# In-memory SQLite for fast tests
aiosqlite>=0.20.0