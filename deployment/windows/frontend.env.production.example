# NEXT_PUBLIC_* values are embedded during npm run build.
# Rebuild after changing this file.

# Local server-only smoke test:
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001

# Recommended with IIS reverse proxy (replace the line above):
# NEXT_PUBLIC_API_BASE_URL=https://YOUR-DOMAIN/api

