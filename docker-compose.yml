# docker-compose.yml — local development only.
# Brings up a Postgres instance on port 5432 with no password for the
# `postgres` superuser. The backend connects via DATABASE_URL.
#
# Usage:
#   docker compose up -d postgres
#   docker compose down
#
# The backend reads RECSYS_DATABASE_URL; defaults to:
#   postgresql+psycopg://postgres:postgres@127.0.0.1:5432/web_rs_thaiarts
services:
  postgres:
    image: postgres:16-alpine
    container_name: thai_arts_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: web_rs_thaiarts
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d web_rs_thaiarts"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data: